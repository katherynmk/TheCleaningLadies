@page
@using RazorPagesUser.Models
@model RazorPagesUser.Pages.UserModel
@{    
    ViewData["Title"] = "Users";
}

<h1>Users</h1>
<form method="post" class="card p-3">
    <div class="row">
        <div asp-validation-summary="All"></div>
    </div>
    <div class="form-group mb-0 align-middle">
        <!--<label asp-for="NewUser.username">User name</label>
        <input type="text" asp-for="NewUser.username" class="mr-5">
         <label asp-for="NewUser.password">Password</label>
        <input type="text" asp-for="NewUser.password" class="mr-5" />
        <label asp-for="NewUser.location">location</label>
        <input type="text" asp-for="NewUser.location" class="mr-5" 
        <button class="btn btn-primary">Add</button>-->
        <form method="post">
            <input name="nusername"/>
            <input name="npassword"/>
            <input name="nlocation"/>
            <input type="submit"/>
        </form>
    </div>
</form>
<table class="table mt-5">
    <thead>
        <tr>
            <th scope="col">User Name</th>
            <th scope="col">Location</th>
            <th scope="col">Delete</th>
        </tr>
    </thead>
            <!--<tr>
            <td>@Model.NewUser.username</td>
            <td>@Model.NewUser.location</td>
            <td>
            </td>-->
        @foreach (var user in Model.users)
        {
            <tr>
                <td>@user.username</td>
                <td>@user.location</td>
                <tr>
                <td>
                    
                </td>
            </tr>
            </tr>
        }
</table>

@section Scripts {
<partial name="_ValidationScriptsPartial" />
}
